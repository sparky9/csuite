# Support Agent environment configuration

# Anthropic API key used for live ticket resolutions
ANTHROPIC_API_KEY=
# Optional override for the Anthropic model
AI_MODEL=claude-3-5-sonnet-20241022

# Path to the SQLite database backing the local RAG store
RAG_DB_PATH=./data/rag-store.sqlite
# Embedding model used by Xenova transformers
RAG_EMBEDDING_MODEL=Xenova/all-MiniLM-L6-v2

# Logging configuration
LOG_LEVEL=info

# Enable to run the agent without calling Anthropic (used by smoke test)
MOCK_SUPPORT_AGENT=1
